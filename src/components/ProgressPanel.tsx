import { BarChart3, Flame, Save } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
import { getAnalytics, getPreferences, savePreferences } from "../lib/api";

export function ProgressPanel() {
  const [analytics, setAnalytics] = useState<Awaited<ReturnType<typeof getAnalytics>> | null>(null); const [preferences, setPreferences] = useState({ dailyGoalMinutes: 20, preferredSessionMinutes: 15 }); const [error, setError] = useState<string | null>(null); const [saved, setSaved] = useState(false);
  useEffect(() => { Promise.all([getAnalytics(), getPreferences()]).then(([summary, settings]) => { setAnalytics(summary); setPreferences(settings.preferences); }).catch((reason) => setError(reason instanceof Error ? reason.message : "Could not load progress.")); }, []);
  const submit = async (event: FormEvent) => { event.preventDefault(); try { const result = await savePreferences(preferences); setPreferences(result.preferences); setSaved(true); setTimeout(() => setSaved(false), 2500); } catch (reason) { setError(reason instanceof Error ? reason.message : "Could not save preferences."); } };
  const maxHeat = Math.max(1, ...(analytics?.heatmap.map((day) => day.count) ?? [0]));
  return <section className="mx-auto mt-6 max-w-6xl rounded-[1.75rem] border border-slate-200 bg-white p-6"><div className="flex items-center gap-2"><BarChart3 className="size-5 text-emerald-700" /><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">Learning signals</p><h2 className="mt-1 text-2xl font-semibold tracking-[-.035em]">Progress and preferences</h2></div></div>{error && <p className="mt-5 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}{analytics ? <><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-emerald-50 p-4"><p className="text-xs font-semibold text-emerald-800">Cards due</p><p className="mt-2 text-3xl font-semibold">{analytics.dueCount}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-600">Retention</p><p className="mt-2 text-3xl font-semibold">{analytics.retentionRate}%</p></div><div className="rounded-xl bg-amber-50 p-4"><p className="flex items-center gap-1 text-xs font-semibold text-amber-800"><Flame className="size-3.5" />Current streak</p><p className="mt-2 text-3xl font-semibold">{analytics.streak} days</p></div></div><div className="mt-6"><p className="text-sm font-semibold">Study heatmap</p><div className="mt-3 grid grid-cols-7 gap-1.5">{analytics.heatmap.map((day) => <div key={day.date} title={`${day.date}: ${day.count} reviews`} className="aspect-square rounded-md" style={{ backgroundColor: day.count ? `rgba(5, 150, 105, ${0.18 + (day.count / maxHeat) * 0.72})` : "#f1f5f9" }} />)}</div><p className="mt-2 text-xs text-slate-500">Each square represents one day of recorded flashcard reviews.</p></div><div className="mt-6 grid gap-5 lg:grid-cols-2"><div><p className="text-sm font-semibold">Module activity</p><ul className="mt-3 divide-y divide-slate-100">{analytics.moduleStats.map((module) => <li className="flex justify-between gap-3 py-2 text-sm" key={module.id}><span className="font-medium">{module.title}</span><span className="text-slate-500">{module.reviews} reviews · {module.sessions} sessions</span></li>)}</ul></div><form className="rounded-xl bg-slate-50 p-4" onSubmit={submit}><p className="text-sm font-semibold">Study preferences</p><label className="mt-3 block text-xs font-semibold text-slate-600">Daily goal (minutes)<input className="mt-1 h-10 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm" type="number" min="5" max="180" value={preferences.dailyGoalMinutes} onChange={(event) => setPreferences({ ...preferences, dailyGoalMinutes: Number(event.target.value) })} /></label><label className="mt-3 block text-xs font-semibold text-slate-600">Preferred session (minutes)<input className="mt-1 h-10 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm" type="number" min="5" max="180" value={preferences.preferredSessionMinutes} onChange={(event) => setPreferences({ ...preferences, preferredSessionMinutes: Number(event.target.value) })} /></label><button className="mt-4 inline-flex items-center gap-2 rounded-lg bg-slate-950 px-3 py-2 text-sm font-semibold text-white"><Save className="size-4" />{saved ? "Saved" : "Save preferences"}</button></form></div></> : <p className="mt-6 text-sm text-slate-500">Loading progress…</p>}</section>;
}
